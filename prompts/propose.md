You are filing one candidate note into a knowledge wiki's sandbox.

1. Write the note content given below to the target path given
   below — exactly those bytes: no rewording, no reformatting, no
   added or dropped lines. Create the directory if it is absent.
2. Write nothing else: no other file, no index.md or log.md change,
   no edit anywhere under wiki/ or raw/. Do not run git commands —
   the wrapper stamps, audits, and commits the note itself.

The wrapper checks every path this run touched: any write outside
wiki/sandbox/ reverts the whole run and fails it. Your reply is
print-only; the one file write is your only output action.
