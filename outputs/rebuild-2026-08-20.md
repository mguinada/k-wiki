# Regeneration Drill Report — k-wiki #10

**Date:** 2026-08-20 · **Data repo:** `~/Lab/k-wiki-data` · **Base commit:** `7f7949d`

## Procedure executed (per `wiki/AGENTS.md`, Regeneration section)

1. Removed wiki content pages (`index.md`, `overview.md`, `log.md`, everything
   under `concepts/`, `entities/`, `sources/`, `comparisons/`, `queries/`).
   Kept `wiki/AGENTS.md`, `.obsidian/`, `.gitkeep` skeletons.
2. Rebuilt from `raw/` via the rebuild prompt (`prompts/rebuild.md`), following
   the contract.
3. Restored the accreted layer: `git restore wiki/queries/`.
4. Spot-check against pre-deletion versions (`git show HEAD:wiki/<path>`) —
   this report.

## Scope (decision B, human-approved)

`raw/` holds 137 synced notes; the pre-drill wiki covered exactly one source.
The drill rebuilt **that coverage only** — the single graph-engineering course
note. The 136 synced-but-uningested notes remain outside wiki coverage
(stated in `overview.md`), awaiting the scheduled ingest (#11). This keeps
the drill the issue intended: "while the wiki is one note deep, exactly so
the procedure is proven cheap."

## Gates

- `npm run check-links -- ~/Lab/k-wiki-data/wiki` → **ok: 39 wikilinks
  resolve across 10 pages** (exit 0), before and after the rebuild.
- Contract compliance: frontmatter on all content pages (title/type/created/
  updated/tags/sources/confidence/status), `log.md` entry in parseable
  `## [YYYY-MM-DD] <operation> | <title>` format, ≥2 wikilinks per page.

## Side-by-side spot-check

| Page | Concepts covered | Sources attributed | Contradictions/open questions | Verdict |
|---|---|---|---|---|
| `sources/graph-engineering-course.md` | Same: 5-step structure, key claims, garbled-clipping note | ✓ same `sources` path | ✓ zero-tokens CONTRADICTION + 16-vs-64 OPEN QUESTION as callouts | Pass |
| `concepts/loop-engineering.md` | Same: cycle, "atom", Goodhart/support-team example, graph-of-loops remedy | ✓ | n/a (none in old) | Pass |
| `concepts/graph-engineering.md` | Same: node/edge vocabulary, edge test, 2 failure modes, anchors, wrong-choice list, scale/cost | ✓ | ✓ 16-vs-64 preserved (on source + entity pages; old had it on this page with a reconciliation hypothesis, not re-derived) | Pass |
| `entities/claude-code.md` | Same: dynamic workflows, plan matrix, `/workflows`, save-with-`s`, 1000/16 scale, cost correction | ✓ | ✓ zero-tokens tension in prose + callout on source page (old had the callout here) | Pass |
| `entities/bun-rust-port.md` | Same: ~50 workflows / 64 parallel / 535k→1M+ lines / 11 days / $165k / supervision / criticism, operational failure, worktree fix | ✓ (+ Willison link, second-hand note) | ✓ OPEN QUESTION 64-vs-16 (without the old "several concurrent workflows" hypothesis) | Pass |
| `comparisons/loop-vs-graph-engineering.md` | Same table shape: unit, shape, failure, blind spot, best-for, wrong-when | ✓ | ✓ shared-foundation point | Pass |
| `index.md` | **Byte-identical to pre-deletion version** — see disclosure below | ✓ | ✓ | Pass |
| `overview.md` | Same coverage + adds explicit 136-not-ingested boundary | ✓ | ✓ both tensions named | Pass |
| `log.md` | Replaced by the rebuild entry (log is in the delete set; prior entries live in git history) | ✓ | ✓ | Pass |
| `queries/loop-failure-graph-fit.md` | Restored from git, byte-identical | — | — | Pass |

**Disclosure — index.md:** the rebuilt `index.md` is byte-identical to the
pre-deletion one. The rebuild session had read the old index earlier (while
planning the drill), so this page is not an independent derivation. The
one-line descriptions are also the natural ones for these pages; treat the
identity as convenient, not as evidence.

## Known losses (regeneration is not identity)

- **Sync-unstable flag lost.** The pre-drill source page flagged the
  16-concurrent figure as sync-unstable (16→32→16 same day, from sync
  history). That knowledge is not derivable from `raw/` content — it is a
  sync-history annotation. Git history retains it; the human may re-add it
  as a correction after review.
- **Log history replaced.** Ingest/namespace/lint/query/update entries
  (2026-08-19 → 2026-08-20) are gone from the working log; the rebuild entry
  records the operation. Per the contract, git history is the record.
- **`created` dates reset** to 2026-08-20 (were 2026-08-19).
- **Reconciliation hypotheses dropped.** The old pages hypothesized the
  64-parallel peak was "several workflows concurrent" and noted vendor
  numbers were unverified against documentation. The rebuilt pages keep the
  open questions but not the speculative reconciliations (arguably cleaner;
  they were inference, not source fact).

## Conclusion

The invariant holds: with the contract preserved and `queries/` restored
from git, the core wiki (6 pages + index/overview/log) regenerates from
`raw/` alone with equivalent concept coverage, source attribution, and
preserved tensions. Cost of the drill: one working session at one-note
depth, as intended.
