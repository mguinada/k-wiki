# Wiki Lint Report — 2026-08-20

Scope: all 9 markdown files under `wiki/` (6 content pages, index, overview, log;
AGENTS.md excluded). `raw/` was never read. Checks follow the 14-point lint
checklist in the task prompt.

## Summary

| # | Check | Result |
|---|-------|--------|
| 1 | Unsupported claims | Pass — all factual claims carry source attribution; single-source vendor-scale numbers already flagged as open questions |
| 2 | Missing source attribution | Pass — all 6 content pages cite the identical raw/ path |
| 3 | Contradictory claims | Pass — the two known tensions (zero-tokens vs usage cost; 16-concurrent cap vs 64 parallel) are preserved with CONTRADICTION/OPEN QUESTION callouts |
| 4 | Duplicate pages | Pass — no overlapping topics |
| 5 | Orphan pages | Pass — every content page has inbound links |
| 6 | Broken wikilinks | **1 issue — FIXED** (see below) |
| 7 | Missing/invalid frontmatter | Pass for content pages; structural pages ambiguous (see below) |
| 8 | Missing required frontmatter fields | Pass — all required fields present per page type |
| 9 | Non-canonical/inconsistent tags | Internally consistent; no canonical vocabulary defined (see below) |
| 10 | Stale or obsolete claims | Undeterminable internally; single-source wiki (see below) |
| 11 | Incorrect page types | Pass — type frontmatter matches directory in all 4 cases |
| 12 | Missing important relationships | Pass — all cross-links present; every page links ≥2 others |
| 13 | Index entries missing from wiki | Pass — index ↔ pages consistent both directions |
| 14 | Excessive filler | Pass — pages are tight; no filler found |

## Fixed (mechanical)

### Broken wikilink `[[@0xCodila]]` — sources/graph-engineering-course.md

Two occurrences (frontmatter `author:` and body) linked to a page that does not
exist. Creating an entity page for the author would violate the stub rule
(term appears in a single source), so the links were de-linked to plain text
`@0xCodila`. `updated` bumped to 2026-08-20. If a second @0xCodila source is
ever ingested, promote to `entities/0xcodila.md` and restore the links.
Post-fix recheck: 0 broken wikilinks wiki-wide.

## Reported (ambiguous — not fixed, no guessing)

### 1. Structural pages lack frontmatter

`index.md`, `overview.md`, and `log.md` have no YAML frontmatter. The contract
says "Every wiki page must use valid Obsidian-compatible YAML frontmatter", but
these are structural pages whose dedicated contract sections (Index, Overview,
Log) specify no frontmatter — and `log.md` is append-only, so prepending
frontmatter would alter its structure. Left as-is pending an owner decision.
If fixed: `title`/`type: meta`/`created`/`updated`/`tags` on index and overview;
log.md arguably should stay untouched.

### 2. No canonical tag vocabulary defined

Tags in use: `ai-agents` (6 pages), `agent-loops` (2), `agent-graphs` (2),
`source` (1), `tools` (1), `claude-code` (1), `case-study` (1), `bun` (1).
They are internally consistent and hierarchically sensible, but the contract
requires "canonical wiki vocabulary" without enumerating one anywhere in the
repo. Cannot lint against an undefined list. Suggestion: define the vocabulary
(e.g. on index.md or a meta page) at next ingestion, then re-lint.

### 3. Staleness not determinable internally

Single-source wiki; the contract's source of truth is `raw/`, so staleness
can only be judged against newer sources, of which there are none. The
time-sensitive claims (Claude Code v2.1.154+, plan availability, 1,000-agent /
16-concurrent caps) are already marked single-source/unverified on
`graph-engineering.md` and `claude-code.md` — correct handling.
External-link liveness only (report-only, not written into the wiki): both
cited URLs (claude.com dynamic-workflows announcement, simonwillison.net Bun
write-up) return HTTP 200 as of 2026-08-20.

## Verified clean

- Frontmatter: valid YAML, all required fields per page type, ISO dates,
  `type` matches directory for all 6 content pages.
- Sources field: all 6 pages cite the identical
  `notes/Engineering/…/Graph Engineering….md` path (consistent with the
  2026-08-19 namespace rename logged in log.md).
- Index: complete and accurate both directions; page counts in overview.md
  match reality (6 pages).
- Log format: both existing entries match the parseable header format.
- Orphans/duplicates: none; every page has ≥2 outbound and ≥1 inbound links.

## Files changed

- `wiki/sources/graph-engineering-course.md` — de-linked 2 `[[@0xCodila]]`
  occurrences; `updated` → 2026-08-20.
- `wiki/log.md` — appended lint entry (separate change).
